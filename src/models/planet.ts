export interface PlanetJson {
  id: number;
  name: string;
  orbitRadius: number;
  speed: number;
  dashSize: number;
  gapSize: number;
  objPath: string;
  mtlPath: string;
  size: number;
  rotateSpeed: number;
}
